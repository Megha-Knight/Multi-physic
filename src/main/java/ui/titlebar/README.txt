The in-window blue Titlebar (title_ui_main.java) was intentionally REMOVED.
The application now uses the DEFAULT OS title bar, which shows the
"Multi-physics" title, the app logo/icon, and the native window controls
(minimize / maximize / close). See application/Main.java.

This folder is kept as a placeholder in case a custom title bar is
reintroduced later.
